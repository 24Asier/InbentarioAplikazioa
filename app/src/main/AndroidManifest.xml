<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-feature android:name="android.hardware.camera" android:required="true"/>
    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED"/>

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.BiltegiApp2">
        <activity android:name=".Activities.Main.MainActivity" android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>
                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>
    <activity android:name=".Activities.SplashActivity" android:exported="true"/>
    <activity android:name=".Activities.Edit.EditProductActivity" android:exported="false"/>
    <activity android:name=".Activities.Profile.ProfileActivity" android:exported="false"/>
    <activity android:name=".Activities.Albaran.AlbaranActivity" android:exported="false"/>
    <activity android:name=".Activities.Inventory.InventoryActivity" android:exported="false"/>
    <receiver android:name=".Activities.BootReceiver" android:exported="true">
        <intent-filter>
            <action android:name="android.intent.action.BOOT_COMPLETED"/>
        </intent-filter>
    </receiver>
</application>

</manifest>